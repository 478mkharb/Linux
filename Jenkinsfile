pipeline {
    agent any

    stages {
        stage('Checkout') {
            steps {
                echo "Code checked out from GitHub"
            }
        }

        stage('Build') {
            steps {
                echo "Running build..."
            }
        }

        stage('Test') {
            steps {
                echo "Running tests..."
            }
        }
    }
}
